Here's a structured technical brief extracted from the provided Lean 4 file on **HNN Extensions of Groups**:

---

### **1. Key Definitions & Theorems**

| Name | Type / Signature | Purpose |
|------|------------------|---------|
| `HNNExtension.con` | `G → A → B → φ → Con (G ∗ ℤᵐ)` | Defines the congruence relation on the coproduct `G ∗ ℤ` generated by relations enforcing `t a t⁻¹ = φ(a)` for `a ∈ A`. |
| `HNNExtension` | `Type u` | The quotient `(G ∗ ℤᵐ) / con`, i.e., the HNN extension group. |
| `of` | `G →* HNNExtension G A B φ` | Canonical monoid (group) homomorphism embedding `G` into the HNN extension. |
| `t` | `HNNExtension G A B φ` | The *stable letter*, representing the adjoined generator `t`. |
| `t_mul_of` | `t * of a = of (φ a) * t` | Core relation: conjugation by `t` implements `φ` on `A`. |
| `equiv_eq_conj` | `of (φ a) = t * of a * t⁻¹` | Equivalent formulation of the defining relation. |
| `lift` | `(f : G →* H) → x : H → (∀ a, x * f a = f (φ a) * x) → HNNExtension →* H` | Universal property: extends a map from `G` and `t` to a homomorphism from the HNN extension. |
| `of_injective` | `Injective of` | The embedding of `G` is injective (proven via Britton’s Lemma and normal forms). |
| `ReducedWord.prod` | `ReducedWord → HNNExtension` | Interprets a reduced word as an element of the HNN extension. |
| `NormalWord` | Structure extending `ReducedWord` | Normal form for elements: head in `G`, list of `(u, g)` with `g` in a fixed transversal, no cancellation patterns. |
| `unitsSMul` | `ℤˣ → NormalWord → NormalWord` | Action of `tᵘ` on normal forms, implementing multiplication and reduction. |
| `equiv` | `HNNExtension ≃ NormalWord d` | Bijection between group elements and normal forms (key for injectivity proofs). |
| `prod_injective` | `Injective (prod φ)` | Normal form representation is unique. |
| `Britton’s Lemma` (via `ReducedWord.toList_eq_nil_of_mem_of_range`) | If `g ∈ G` has reduced word with no `t`, then `g = 1` iff word is empty. | Critical for proving `of` is injective. |

---

### **2. Naming Conventions**

- **Prefixes**:
  - `of_`: canonical embedding from base group `G`.
  - `t_`: stable letter-related operations (`t_mul_of`, `inv_t_mul_of`, `t_pow_smul_eq_unitsSMul`).
  - `unitsSMul_`: action of `tᵘ` on normal forms (`unitsSMul`, `unitsSMulEquiv`, `unitsSMul_neg`).
  - `toSubgroup_`, `toSubgroupEquiv_`: parameterized by `u : ℤˣ = {1, -1}` to unify cases for `A`/`B` and `φ`/`φ⁻¹`.
  - `cons`, `consRecOn`: constructors/induction principles for `NormalWord`.
  - `prod_`: interpretation of words as group elements.

- **Suffixes**:
  - `_equiv`: isomorphisms (`equiv`, `unitsSMulEquiv`).
  - `_hom`: monoid/group homomorphisms (`lift`, `of`).
  - `_def`: definitional lemmas (`group_smul_def`, `unitsSMulGroup_snd`).
  - `_mem`, `_chain`: properties of list components in `ReducedWord`/`NormalWord`.

---

### **3. Tactic Stack**

Frequently used tactics in proofs:
- `simp` / `simp_rw`: Simplification of group equations, especially using `equiv_eq_conj`, `of_mul_t`, etc.
- `rw`: Rewriting with defining relations (`t_mul_of`, `equiv_eq_conj`, `unitsSMul_neg`).
- `induction ... using ...`: Structural induction on `NormalWord` (`consRecOn`), group elements (`induction_on`).
- `rcases` / `cases`: Decomposing existential hypotheses or `ℤˣ` cases (`Int.units_eq_one_or`).
- `dsimp`, `conv_lhs`: Focused simplification in complex expressions (e.g., after `erw`).
- `erw`: Rewriting with definitional equality (used for `equiv_snd_eq_inv_mul`, etc.).
- `aesop` / `tauto`: Automated reasoning in simple logical steps (e.g., `Cancels` definitions).
- `ext`: Extensionality for group homomorphisms (`hom_ext`).

---

### **4. Proof Logic**

- **Structure of proofs**:
  1. **Normal form construction**: Use transversals to define `NormalWord`, prove existence (`exists_normalWord_prod_eq`) and uniqueness (`prod_injective`).
  2. **Action simulation**: Define `unitsSMul` to mimic multiplication by `tᵘ`, prove correctness via `prod_unitsSMul`.
  3. **Britton’s Lemma**: Prove via normal forms — if a reduced word in `G` equals `1`, it must be empty (no `t` letters).
  4. **Injectivity of `of`**: Reduce to Britton’s Lemma using `equiv` and `prod_injective`.
  5. **Universal property**: `lift` is constructed via `Con.lift`, verified via `lift_t`, `lift_of`, and `hom_ext`.

- **Induction patterns**:
  - `NormalWord` uses `consRecOn` (induction on list length with `cons` constructor).
  - `HNNExtension` uses `induction_on` (generated from `of`, `t`, closure under `*`, `⁻¹`).

- **Case analysis**:
  - `u : ℤˣ` splits into `1` or `-1` (handled via `Int.units_eq_one_or`).
  - Cancellation cases (`Cancels u w`) vs. non-cancellation (`¬ Cancels u w`) in `unitsSMul`.

---

### **5. Imports & Scope**

**Primary dependencies**:
- `Mathlib.Algebra.Ring.Int.Units`: For `ℤˣ` structure.
- `Mathlib.GroupTheory.Coprod.Basic`: Coproduct (free product) of groups.
- `Mathlib.GroupTheory.Complement`: For transversals and complements (`IsComplement`).

**Domain scope**:
- **Group theory**, specifically:
  - Free products and amalgamated constructions.
  - Normal forms and decision problems (Britton’s Lemma).
  - Universal properties of HNN extensions.
- **Foundational tools**:
  - Quotients by congruences (`Con`).
  - MulAction and SMul for group actions on normal forms.

---

This summary captures the formalization’s architecture, emphasizing the interplay between syntactic normal forms (`NormalWord`), semantic group operations (`HNNExtension`), and the logical machinery (congruences, induction, case analysis) used to establish correctness.