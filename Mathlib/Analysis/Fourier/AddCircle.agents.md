Here's a structured technical brief extracted from the provided Lean 4 file:

---

### **Technical Brief: Fourier Analysis on the Additive Circle (`AddCircle T`)**

#### **1. Key Definitions & Theorems**

| Name | Type / Purpose |
|------|----------------|
| `haarAddCircle` | `Measure (AddCircle T)` — Haar measure normalized to total mass 1. |
| `fourier (n : ℤ)` | `C(AddCircle T, ℂ)` — Continuous monomial `x ↦ exp(2πi n x / T)`. |
| `fourierLp p n` | `Lp ℂ p haarAddCircle` — Image of `fourier n` in `Lᵖ` space. |
| `fourierCoeff f n` | `E` — `n`-th Fourier coefficient of `f : AddCircle T → E`, defined as `∫ fourier (-n) • f dhaarAddCircle`. |
| `fourierCoeffOn hab f n` | `E` — Fourier coefficient of `f : ℝ → E` on interval `(a, b]` (via periodic extension). |
| `fourierSubalgebra` | `StarSubalgebra ℂ C(AddCircle T, ℂ)` — Star-subalgebra generated by `{fourier n | n : ℤ}`. |
| `fourierBasis` | `HilbertBasis ℤ ℂ (Lp ℂ 2 haarAddCircle)` — Orthonormal Hilbert basis given by `fourierLp 2`. |
| `span_fourier_closure_eq_top` | `Submodule.topologicalClosure (span ℂ (range fourier)) = ⊤` — Density of trigonometric polynomials in `C(AddCircle T, ℂ)`. |
| `span_fourierLp_closure_eq_top` | `topologicalClosure (span ℂ (range fourierLp p)) = ⊤` — Density in `Lᵖ` for `1 ≤ p < ∞`. |
| `orthonormal_fourier` | `Orthonormal ℂ (fourierLp 2)` — Monomials are orthonormal in `L²`. |
| `hasSum_fourier_series_L2` | `HasSum (fourierCoeff f i • fourierLp 2 i) f` — Fourier series converges to `f` in `L²`. |
| `tsum_sq_fourierCoeff` | `∑' i, ‖fourierCoeff f i‖² = ‖f‖₂²` — Parseval’s identity. |
| `hasSum_fourier_series_of_summable` | `HasSum (fourierCoeff f i • fourier i) f` — Uniform convergence if coefficients are summable. |
| `fourierCoeffOn_of_hasDerivAt` | Relates Fourier coefficients of `f` and `f'` (integration by parts formula). |

---

#### **2. Naming Conventions**

- **Prefixes**:
  - `fourier_`: for monomials, coefficients, and related constructions (`fourier`, `fourierCoeff`, `fourierLp`, `fourierBasis`, `fourierSubalgebra`).
  - `haarAddCircle`: for normalized Haar measure on the circle.
  - `fourierCoeffOn`: for Fourier coefficients on intervals (via periodic extension).
- **Suffixes**:
  - `_apply`, `_coe_apply`, `_apply'`: for evaluation lemmas (e.g., `fourier_apply`, `fourier_coe_apply`).
  - `_closure_eq_top`: for density results.
  - `_eq_intervalIntegral`: for expressing integrals over `AddCircle T` as interval integrals.
  - `_of_hasDerivAt`: for derivative-based Fourier coefficient identities.
- **Special**:
  - `'` suffix (e.g., `fourier_zero'`, `fourier_neg'`) often denotes a variant of a lemma in terms of `toCircle` or `zsmul`.
  - `repr` suffix (e.g., `fourierBasis_repr`) for coordinate maps in basis expansions.

---

#### **3. Tactic Stack**

Frequently used tactics in proofs:
- `rw`, `simp_rw`, `simp`: for rewriting and simplification (especially with `fourier_coe_apply`, `fourier_zero`, `fourier_neg`, etc.).
- `induction` + `QuotientAddGroup.induction_on`: for reasoning about elements of `AddCircle T`.
- `convert`, `congr`: for equating integrals or expressions up to almost-everywhere equality.
- `filter_upwards`, `ae_of_all`: for working with almost-everywhere statements in measure theory.
- `ring`, `field_simp`, `norm_num`: for algebraic simplifications in complex arithmetic.
- `exact`, `apply`, `refine`: for constructing proofs step-by-step.
- `have`, `set`, `convert`: for intermediate lemmas and substitutions.
- `continuous_induced_dom`, `continuous_zsmul`, `continuous_toCircle`: for continuity arguments.
- `intervalIntegral.integral_congr_ae`, `setIntegral_congr_fun`: for manipulating interval integrals.

---

#### **4. Proof Logic & Strategy**

- **Density proofs**:
  - Use **Stone–Weierstrass** to show the star-algebra generated by `fourier n` separates points and is closed under conjugation ⇒ dense in `C(AddCircle T, ℂ)`.
  - Lift to `Lᵖ` via density of continuous functions in `Lᵖ` and continuity of `toLp`.
- **Orthonormality in `L²`**:
  - Compute inner products using `fourier_add`, `fourier_neg`, and `integral_eq_zero_of_add_right_eq_neg` (via symmetry under translation by `T/(2n)`).
- **Convergence results**:
  - `L²` convergence follows from Hilbert basis theory (`fourierBasis`).
  - Uniform convergence follows from summability of coefficients + `ContinuousMap.hasSum_of_hasSum_Lp`.
- **Derivative identities**:
  - Use integration by parts (`integral_mul_deriv_eq_deriv_mul_of_hasDeriv_right`) with antiderivatives of `fourier (-n)`.
- **Measure-theoretic reductions**:
  - Replace integrals over `AddCircle T` with integrals over `[a, a+T]` using `fourierCoeff_eq_intervalIntegral` and `volume_eq_smul_haarAddCircle`.

---

#### **5. Imports & Scope**

**Primary Dependencies**:
- `Mathlib.Analysis.SpecialFunctions.ExpDeriv`: exponential function derivatives.
- `Mathlib.Analysis.SpecialFunctions.Complex.Circle`: `toCircle`, `exp` on circle.
- `Mathlib.Analysis.InnerProductSpace.l2Space`: `L²` theory, Hilbert bases.
- `Mathlib.MeasureTheory.Function.ContinuousMapDense`: density of continuous maps in `Lᵖ`.
- `Mathlib.MeasureTheory.Function.L2Space`: `L²` inner product, orthonormal bases.
- `Mathlib.MeasureTheory.Group.Integral`: integration on quotient groups (e.g., `AddCircle T = ℝ / Tℤ`).
- `Mathlib.MeasureTheory.Integral.Periodic`: periodic functions and integrals.
- `Mathlib.Topology.ContinuousMap.StoneWeierstrass`: Stone–Weierstrass theorem.
- `Mathlib.MeasureTheory.Integral.FundThmCalculus`: fundamental theorem of calculus for interval integrals.

**Key Variables & Assumptions**:
- `T : ℝ`, with `0 < T` (enforced via `Fact (0 < T)`).
- `E` is a complete normed `ℂ`-vector space (for vector-valued functions).
- `p : ℝ≥0∞`, with `1 ≤ p < ∞` for `Lᵖ` density results.

---

Let me know if you'd like a visual dependency graph or a summary of proof dependencies (e.g., which theorems rely on `orthonormal_fourier`).