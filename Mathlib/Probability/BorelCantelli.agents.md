Here's a structured technical brief extracted from the provided Lean 4 file:

---

### **Technical Brief: Second Borel–Cantelli Lemma in Lean 4**

#### **1. Key Definitions & Theorems**

| Name | Type / Signature | Purpose |
|------|------------------|---------|
| `iIndepFun.indep_comap_natural_of_lt` | `∀ i, StronglyMeasurable (f i) → iIndepFun (fun _ => mβ) f μ → i < j → Indep (MeasurableSpace.comap (f j) mβ) (Filtration.natural f hf i) μ` | Shows independence between the σ-algebra generated by `f j` and the natural filtration up to time `i`, under independence of the family `f`. |
| `iIndepFun.condexp_natural_ae_eq_of_lt` | `[SecondCountableTopology β] → [CompleteSpace β] → [NormedSpace ℝ β] → (∀ i, StronglyMeasurable (f i)) → iIndepFun (fun _ => mβ) f μ → i < j → μ[f j | Filtration.natural f hf i] =ᵐ[μ] fun _ => μ[f j]` | Conditional expectation of `f j` w.r.t. earlier natural filtration is almost everywhere constant (equal to its unconditional expectation), due to independence. |
| `iIndepSet.condexp_indicator_filtrationOfSet_ae_eq` | `(∀ n, MeasurableSet (s n)) → iIndepSet s μ → i < j → μ[(s j).indicator (1 : Ω → ℝ) | filtrationOfSet hsm i] =ᵐ[μ] fun _ => (μ (s j)).toReal` | Conditional expectation of indicator of `s j` w.r.t. earlier filtration of sets is a.s. constant equal to `μ(s j)`. Core technical lemma for martingale argument. |
| `measure_limsup_eq_one` | `{s : ℕ → Set Ω} → (∀ n, MeasurableSet (s n)) → iIndepSet s μ → (∑' n, μ (s n)) = ∞ → μ (limsup s atTop) = 1` | **Main theorem**: Second Borel–Cantelli lemma — if independent events have divergent total measure, then almost surely infinitely many occur. |

#### **2. Naming Conventions**

- **Prefixes**:
  - `iIndepFun.`: For properties of *independent families of functions*.
  - `iIndepSet.`: For *independent families of sets*.
  - `condexp_`: Conditional expectation-related lemmas.
  - `measurableSet_`, `stronglyMeasurable_`: Measurability conditions.
- **Suffixes**:
  - `_of_lt`: Lemmas parameterized by an inequality `i < j`.
  - `_ae_eq`: Statements about equality almost everywhere.
  - `_natural`: Refers to *natural filtrations* (e.g., `Filtration.natural`, `filtrationOfSet`).
- **Other patterns**:
  - `indicator`: Used for functions like `(s j).indicator`.
  - `tendsto`, `limsup`, `Finset.sum_range`: Standard analysis notation.

#### **3. Tactic Stack**

Frequently used tactics in proofs:
- `rw`: Rewriting using equalities/definitions (e.g., `Filtration.filtrationOfSet_eq_natural`).
- `simp only [...]`: Simplification with precise lemmas (e.g., `integral_indicator_const`, `mul_one`).
- `refine`: Constructing proof terms stepwise.
- `filter_upwards`: For proving statements almost everywhere using `ae_all_iff`.
- `tendsto_atTop_atTop_of_monotone'`: For proving divergence to `∞` of monotone sequences.
- `aesop`, `ring`, `linarith`: Likely used implicitly in subgoals (not explicitly shown but standard in such contexts).
- `exact`, `rwa`, `convert`: For closing goals via known facts or conversions.

#### **4. Proof Logic**

The proof of `measure_limsup_eq_one` follows this logical flow:

1. **Reduction via almost-everywhere equivalence**:
   - Use `measure_congr` and `ae_mem_limsup_atTop_iff` to replace `limsup s` with a set defined via convergence of partial sums of conditional expectations.

2. **Martingale setup**:
   - Define filtration `filtrationOfSet hsm k` generated by `s 0, ..., s k`.
   - Use `iIndepSet.condexp_indicator_filtrationOfSet_ae_eq` to show that the increments `μ[(s (k+1)).indicator | filtration_k]` are a.s. constant = `μ(s (k+1))`.

3. **Apply Lévy’s generalized Borel–Cantelli**:
   - Show that the sum of conditional expectations diverges a.e., using the assumption `∑' n, μ(s n) = ∞`.
   - Use monotonicity and properties of `ENNReal.toReal` to lift divergence from extended reals to reals.

4. **Conclude measure = 1**:
   - Since the limit of partial sums diverges to `∞` a.e., the limsup set has full measure.

#### **5. Imports & Scope**

- **Core imports**:
  - `Mathlib.Probability.Martingale.BorelCantelli`: General Borel–Cantelli theory.
  - `Mathlib.Probability.ConditionalExpectation`: Conditional expectations, filtrations, independence.
  - `Mathlib.Probability.Independence.Basic`: Definitions of independent sets/functions, `iIndepSet`, `iIndepFun`.

- **Domain scope**:
  - Probability theory on measurable spaces.
  - Relies on measure-theoretic tools: conditional expectation, filtrations, independence, limsup of sets.
  - Works in general probability spaces (not necessarily Polish or standard Borel), but uses `SecondCountableTopology`, `CompleteSpace`, `NormedSpace ℝ` for vector-valued functions (used in intermediate lemmas).

---

Let me know if you'd like a diagram of the proof structure or a formalized dependency graph.