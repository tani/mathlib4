Here's a structured technical metadata brief extracted from the provided Lean 4 file:

---

### **Technical Metadata Brief**

#### **1. Key Definitions & Theorems**
| Name | Type / Purpose |
|------|----------------|
| `IsIntegralHom` | A class `Scheme → Scheme → Prop` defining a morphism `f : X ⟶ Y` as *integral* if: <br>• `f` is affine (`IsAffineHom f`), and <br>• for every affine open `U ⊆ Y`, the induced ring map `f.app U .hom` is integral (`RingHom.IsIntegral`). |
| `isIntegralHom_iff` | Generated by `@[mk_iff]`; characterizes `IsIntegralHom f` equivalently via affine covers (not explicitly stated but implied by usage). |
| `hasAffineProperty` | Instance showing `IsIntegralHom` satisfies the `HasAffineProperty` framework, using `affineAnd RingHom.IsIntegral`. |
| `isStableUnderComposition`, `isStableUnderBaseChange`, `containsIdentities`, `isMultiplicative` | Instances proving stability properties of integral morphisms under composition, base change, identity, and finite products (via `IsMultiplicative`). |

#### **2. Naming Conventions**
- **Class names**: `IsIntegralHom`, `IsAffineHom` — predicate-style with `Is_` prefix.
- **Property instances**: `hasAffineProperty`, `isStableUnderComposition`, etc. — use `isStableUnder_`, `containsIdentities`, `hasAffineProperty`.
- **Helper lemmas**: `affineAnd_isStableUnder_`, `isIntegral_ofLocalizationSpan`, `isIntegral_respectsIso`, `isIntegral_isStableUnderBaseChange`, `isIntegral_stableUnderComposition` — follow pattern `Q_of_` or `Q_isStableUnder_`, where `Q = RingHom.IsIntegral`.
- **Method suffixes**: `_of_`, `_respectsIso`, `_isStableUnder_`, `_ofLocalizationSpan`.

#### **3. Tactic Stack**
- **Core tactics**: `simp`, `rw`, `show`, `by`, `exact`, `refine`.
- **Domain-specific automation**:
  - `simp` with custom lemmas (`isIntegralHom_iff`, `RingHom.isIntegral_*`).
  - `rw [HasAffineProperty.affineAnd_iff ...]` — rewrites using structural lemmas for `HasAffineProperty`.
  - `simpa using ...` — simplifies and discharges goals using a given proof.
  - `ring`/`abel` not used here (algebraic geometry context, not commutative algebra proofs *per se*).
  - No heavy `aesop` or `linarith`; relies on structured algebraic geometry libraries.

#### **4. Proof Logic**
- **High-level strategy**:
  - Leverages the `HasAffineProperty` interface to reduce global properties (e.g., stability under base change) to local (affine) checks.
  - Uses known stability properties of `RingHom.IsIntegral` (e.g., `isIntegral_stableUnderComposition`, `isIntegral_isStableUnderBaseChange`) as inputs to generic `HasAffineProperty.*` lemmas.
  - Proofs are largely *modular*: composition/base change instances are derived by applying generic infrastructure (`affineAnd_isStableUnder_...`) to verified ring-theoretic facts.
- **Typical proof flow**:
  ```lean
  instance : ... := 
    HasAffineProperty.affineAnd_isStableUnder_... 
      (Q := RingHom.IsIntegral) 
      hasAffineProperty 
      [ring-theoretic stability instance]
  ```

#### **5. Imports**
| Module | Role |
|--------|------|
| `Mathlib.AlgebraicGeometry.Morphisms.AffineAnd` | Provides `HasAffineProperty` and `affineAnd` infrastructure for combining affine and property `Q`. |
| `Mathlib.AlgebraicGeometry.Morphisms.ClosedImmersion` | Likely imported for context (though not directly used here); part of morphism library. |
| `Mathlib.RingTheory.RingHom.Integral` | Supplies `RingHom.IsIntegral` and its stability properties (e.g., under localization, composition, base change). |

---

### **Summary**
This file formalizes **integral morphisms of schemes** as a stable, affine-local property using Lean’s `Scheme` and `HasAffineProperty` infrastructure. It leverages existing ring-theoretic results about integral ring extensions to derive scheme-theoretic properties via a modular, infrastructure-driven approach. The design reflects the *relative* nature of algebraic geometry: properties are checked on affine opens and glued via stability conditions.

Let me know if you'd like a formalized version of the TODO item ("integral = universally closed + affine") or a comparison with finite morphisms.